import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "Acadify - Academic Document Creator for NEUB Students",
  description:
    "Create professional cover pages for your assignments and lab reports in seconds. Acadify is a free tool designed for North East University Bangladesh (NEUB) students to generate university-standard academic documents with live preview and instant PDF export.",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
